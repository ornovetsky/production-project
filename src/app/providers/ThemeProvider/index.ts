import { useTheme } from "./lib/useTheme";
import ThemeProvider from "./ui/ThemeProvider";

export {
    ThemeProvider,
    useTheme,
}

// этот индекс файл и есть паблик апи который отдает то что мы хотим чтобы было доступно извне